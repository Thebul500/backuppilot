apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: backuppilot

resources:
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: backuppilot
  app.kubernetes.io/part-of: infrastructure

images:
  - name: backuppilot
    newName: backuppilot
    newTag: latest
