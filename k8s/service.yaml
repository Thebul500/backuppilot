apiVersion: v1
kind: Service
metadata:
  name: backuppilot
  labels:
    app: backuppilot
spec:
  selector:
    app: backuppilot
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
  type: ClusterIP
